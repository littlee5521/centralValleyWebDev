import { Inter, Roboto, Merriweather, Raleway } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });

export const merriweather = Merriweather({
  weight: ["300", "700"],
  subsets: ["latin"],
});

export const raleway = Raleway({
  weight: ["400", "700"],
  subsets: ["latin"],
});
